# Gradle
.gradle/
build/
run/

# Local release artifacts (generated by build / CI)
dist/*.jar
dist/**/*.jar
dist/changelog.md

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.classpath
.project
.settings/
bin/

# OS
.DS_Store
Thumbs.db

# local deploy script (machine-specific Prism paths + WinSCP sessions)
deploy.sh

# local reference notes (untracked)
universal-jar-loading.txt

# local dev session script (untracked)
oc.sh

# local dev session transcript (untracked)
session.json

# local release secrets (GPG-encrypted vault + any decrypted copy)
.release-env
.release-env.gpg

# python bytecode
__pycache__/
*.pyc
