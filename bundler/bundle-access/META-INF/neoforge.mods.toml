modLoader="javafml"
loaderVersion="[1,)"
license="@@LICENSE@@"

[[mods]]
modId="mc_storage_area_network_bundle_access"
version="@@VERSION@@"
displayName="MC Storage Area Network (Remote Access Terminal)"
description='''
One-file install: the MC Storage Area Network API plus Remote Access Terminal. Works on NeoForge; each loader loads only its own nested jars.
'''
