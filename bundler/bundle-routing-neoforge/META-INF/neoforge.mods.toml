modLoader="javafml"
loaderVersion="[1,)"
license="@@LICENSE@@"

[[mods]]
modId="mc_storage_area_network_bundle_routing_neoforge"
version="@@VERSION@@"
displayName="MC Storage Area Network (Routing) (NeoForge)"
description='''
One-file install: the MC Storage Area Network API plus Network Routing. Works on NeoForge; each loader loads only its own nested jars.
'''
