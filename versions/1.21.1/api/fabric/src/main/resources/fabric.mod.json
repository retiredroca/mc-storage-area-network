{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${version}",
  "name": "${mod_name}",
  "description": "Shared item-source API for Storage Network, Crafting Network and shulker-box add-ons. Provides chunk storage scanning, item-source registration and shulker-box helpers so other mods can integrate without depending on either gameplay mod.",
  "authors": ["${mod_authors}"],
  "license": "${mod_license}",
  "environment": "*",
  "mixins": [
    "mc_storage_area_network.mixins.json"
  ],
  "entrypoints": {
    "main": [
      "com.retiredroca.mcstorageareanetwork.fabric.McStorageAreaNetwork"
    ],
    "client": [
      "com.retiredroca.mcstorageareanetwork.fabric.McStorageAreaNetworkClient"
    ]
  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-api": "*",
    "minecraft": "${minecraft_version}",
    "java": ">=21"
  }
}
