# These apply when this directory is built directly (`-p <this dir>`); the composite build
# reads the repo-root gradle.properties instead. Keep both jvmargs values identical so
# one daemon can serve either invocation.
org.gradle.jvmargs=-Xmx12G -XX:MaxMetaspaceSize=1G -XX:+UseParallelGC
org.gradle.java.home=C:\\Users\\Ric\\.gradle\\jdks\\eclipse_adoptium-21-amd64-windows.2
